# 📱 App Structure & Visual Guide

## 🗂️ Complete Directory Structure

```
userapp/
├── android/
│   ├── app/
│   │   ├── build.gradle.kts              (Updated with Firebase)
│   │   └── google-services.json          (Firebase config - template)
│   ├── build.gradle.kts                  (Updated with Google Services)
│   └── gradle/
├── ios/
├── lib/
│   ├── config/
│   │   └── app_colors.dart              ✅ (Primary, Light, Accent colors)
│   ├── models/
│   │   ├── user_model.dart              ✅ (User data + serialization)
│   │   └── restaurant_model.dart        ✅ (Restaurant data model)
│   ├── data/
│   │   └── restaurant_data.dart         ✅ (5 restaurant list)
│   ├── services/
│   │   ├── auth_service.dart            ✅ (Firebase Auth & signup/login)
│   │   └── database_service.dart        ✅ (Firestore operations)
│   ├── screens/
│   │   ├── auth/
│   │   │   ├── login_page.dart          ✅ (Login UI + Logic)
│   │   │   └── registration_page.dart   ✅ (Registration UI + Logic)
│   │   ├── home/
│   │   │   ├── home_page.dart           ✅ (Home + Navigation)
│   │   │   └── dashboard_page.dart      ✅ (Dashboard + Leaderboard)
│   │   └── profile/
│   │       └── profile_page.dart        ✅ (Profile + QR Code)
│   ├── main.dart                        ✅ (App entry + Auth wrapper)
│   └── firebase_options.dart            ✅ (Firebase config - template)
├── test/
├── pubspec.yaml                         ✅ (Updated with dependencies)
├── COMPLETE_README.md                   ✅ (Full project documentation)
├── SETUP_GUIDE.md                       ✅ (Setup instructions)
├── IMPLEMENTATION_GUIDE.md              ✅ (Detailed implementation)
├── QUICK_REFERENCE.md                   ✅ (Command & code reference)
└── analysis_options.yaml
```

## 🎯 Screen Flow Diagram

```
┌─────────────────────────────────────────────────────────┐
│                      APP START                           │
│                                                           │
│  1. Firebase Initialization                             │
│  2. AuthWrapper checks auth state                       │
│  3. Routes to appropriate screen                        │
└─────────────────────────────────────────────────────────┘
         │
    ┌────┴────┐
    │          │
    ↓          ↓
┌────────┐  ┌──────────┐
│ LogIn  │  │ Register │
│  Page  │  │  Page    │
└────────┘  └──────────┘
    │           │
    └─────┬─────┘
          │
          ↓
    ┌──────────┐
    │ HOME     │
    │  PAGE    │
    └──────────┘
       │ │ │
   ┌───┘ │ └───┐
   │     │     │
   ↓     ↓     ↓
┌───┐ ┌───┐ ┌───────┐
│ 1 │ │ 2 │ ...     │ ← 5 Restaurant Cards
└───┘ └───┘ └───────┘
   │ (all lead to)
   ↓
┌────────────┐
│ DASHBOARD  │
│  PAGE      │
│ - Rank     │
│ - Score    │
│ - Rank     │
│ - Points   │
│ - Leaders  │
└────────────┘

And via bottom nav:
         │
         ↓
    ┌──────────┐
    │ PROFILE  │
    │  PAGE    │
    │ - Info   │
    │ - Stats  │
    │ - QR     │
    └──────────┘
```

## 🎨 UI Components Breakdown

### LoginPage (`login_page.dart`)
```
┌──────────────────────────────────┐
│  [Gradient Background - Blue]    │
│                                  │
│         📱 Icon                  │
│       LOGIN                      │
│                                  │
│  ┌────────────────────────────┐  │
│  │ Email Input Field          │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ Password Input Field  👁️   │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │  [LOGIN BUTTON - Yellow]   │  │
│  └────────────────────────────┘  │
│                                  │
│  Don't have account? Sign Up →   │
└──────────────────────────────────┘
```

### RegistrationPage (`registration_page.dart`)
```
┌──────────────────────────────────┐
│  [Gradient Background - Blue]    │
│                                  │
│       📋 Icon                    │
│       SIGN UP                    │
│                                  │
│  ┌────────────────────────────┐  │
│  │ Name Input Field           │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ Email Input Field          │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ Password Input Field  👁️   │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ Confirm Pass Field    👁️   │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │ [SIGN UP BUTTON - Yellow]  │  │
│  └────────────────────────────┘  │
│                                  │
│  Already have account? Login →   │
└──────────────────────────────────┘
```

### HomePage (`home_page.dart`)
```
┌──────────────────────────────────┐
│  [Gradient Background - Blue]    │
│                                  │
│  Welcome! 👋          Logout 🚪  │
│  Explore Restaurants             │
│                                  │
│  ┌──────────────────────────┐   │
│  │ 🍝 Restaurant 1          │   │
│  │ Italian Cuisine          │   │
│  │ [View Dashboard →]       │   │
│  └──────────────────────────┘   │
│                                  │
│  ┌──────────────────────────┐   │
│  │ 🍜 Restaurant 2          │   │
│  │ Asian Fusion             │   │
│  │ [View Dashboard →]       │   │
│  └──────────────────────────┘   │
│                                  │
│  (3 more restaurant cards)       │
│                                  │
│  ┌──────────────────────────┐   │
│  │ 🏠  Dashboard  📊  Profile│   │ ← Bottom Nav
│  └──────────────────────────┘   │
└──────────────────────────────────┘
```

### DashboardPage (`dashboard_page.dart`)
```
┌──────────────────────────────────┐
│  [Gradient Background - Blue]    │
│                                  │
│  Dashboard                       │
│  Restaurant 1                    │
│                                  │
│  ┌──────────────────────────┐   │
│  │ 🏆 Leaderboard Rank      │   │
│  │ #5                       │   │
│  └──────────────────────────┘   │
│                                  │
│  ┌──────────────────────────┐   │
│  │ ⭐ Current Score         │   │
│  │ 250                      │   │
│  └──────────────────────────┘   │
│                                  │
│  ┌──────────────────────────┐   │
│  │ 📈 Highest Score         │   │
│  │ 500                      │   │
│  └──────────────────────────┘   │
│                                  │
│  ┌──────────────────────────┐   │
│  │ 💰 Remaining Points      │   │
│  │ 75                       │   │
│  └──────────────────────────┘   │
│                                  │
│  Top Performers                  │
│  ┌──────────────────────────┐   │
│  │ 1 John    ⭐ 1000        │   │
│  │ 2 Jane    ⭐ 950         │   │
│  │ 3 Mike    ⭐ 900         │   │
│  │ 4 Sarah   ⭐ 850         │   │
│  │ 5 Tom     ⭐ 800         │   │
│  └──────────────────────────┘   │
│                                  │
│  ┌──────────────────────────┐   │
│  │ 🏠  Dashboard  📊  Profile│   │ ← Bottom Nav
│  └──────────────────────────┘   │
└──────────────────────────────────┘
```

### ProfilePage (`profile_page.dart`)
```
┌──────────────────────────────────┐
│  [Gradient Background - Blue]    │
│                                  │
│  My Profile            Logout 🚪 │
│                                  │
│         ⭕ Avatar                │
│       John Doe                   │
│    john@example.com              │
│                                  │
│  ┌─────────┬─────────────────┐  │
│  │ ⭐ 250  │ 📈 500          │  │
│  │ Current │ Highest         │  │
│  │ Score   │ Score           │  │
│  └─────────┴─────────────────┘  │
│  ┌─────────┬─────────────────┐  │
│  │ 💰 75   │ 📅 02/09/2024   │  │
│  │ Points  │ Member Since    │  │
│  └─────────┴─────────────────┘  │
│                                  │
│  My QR Code                      │
│  ┌──────────────────────────┐   │
│  │                          │   │
│  │      ▀▄ ▀▄ ▀▄          │   │
│  │      █████████          │   │
│  │      █▀   █▀            │   │
│  │      █████████          │   │
│  │      ▄▀ ▄▀ ▄▀          │   │
│  │                          │   │
│  │  User ID: abc123xyz     │   │
│  └──────────────────────────┘   │
│                                  │
│  ┌──────────────────────────┐   │
│  │ [EDIT PROFILE - Yellow]  │   │
│  └──────────────────────────┘   │
│                                  │
│  ┌──────────────────────────┐   │
│  │ 🏠  Dashboard  📊  Profile│   │ ← Bottom Nav
│  └──────────────────────────┘   │
└──────────────────────────────────┘
```

## 🔄 Data Flow Architecture

```
┌────────────────────┐
│  User Registration │
└─────────┬──────────┘
          │
          ↓
   ┌─────────────┐
   │ AuthService │
   │  signUp()   │
   └─────────────┘
          │
          ├─ Create Firebase Auth User
          ├─ Create Firestore Document
          │  (uid, email, name, score: 0,
          │   highScore: 0, points: 100)
          └─ Return UserModel
                │
                ↓
          ┌──────────────┐
          │ HomePage     │
          │ (Navigate)   │
          └──────────────┘
```

```
┌───────────────────┐
│  Dashboard View   │
└────────┬──────────┘
         │
         ├─────────────────────────────┐
         │                             │
         ↓                             ↓
    ┌──────────┐            ┌──────────────────┐
    │ Database │            │ Stream User Data │
    │ Service  │            │ (Real-time)      │
    │ getUser()│            └──────────────────┘
    └──────────┘                      │
         │                             │
         ├─────────────────────────────┤
         │                             │
         ↓                             ↓
    ┌──────────┐            ┌──────────────────┐
    │ Firestore│            │ Update UI with   │
    │ /users   │            │ Current Stats    │
    └──────────┘            └──────────────────┘
```

```
┌──────────────────────┐
│ Leaderboard Display  │
└──────────┬───────────┘
           │
           ↓
    ┌──────────────┐
    │ StreamBuilder│
    │ Leaderboard  │
    │ Stream       │
    └──────────────┘
           │
           ↓
    ┌──────────────┐
    │ Firestore    │
    │ /users       │
    │ ORDER BY     │
    │ highScore    │
    │ DESC LIMIT10 │
    └──────────────┘
           │
           ↓
    ┌──────────────┐
    │ Return List  │
    │ of Top Users │
    └──────────────┘
           │
           ↓
    ┌──────────────┐
    │ Display in   │
    │ ListView     │
    └──────────────┘
```

## 📊 Data Models

### UserModel
```dart
class UserModel {
  String uid              // Firebase Auth UID
  String email            // User email
  String name             // Full name
  int score               // Current score
  int highScore           // Best score
  int remainingPoints     // Available points
  String? profileImageUrl // Avatar (optional)
  DateTime createdAt      // Account creation date
}
```

### RestaurantModel
```dart
class RestaurantModel {
  String id               // Restaurant ID
  String name             // Restaurant name
  String description      // Description
  String imageUrl         // Image/icon URL
}
```

## 🎯 Key Implementation Details

### Responsive Sizing (Sizer)
```dart
// Width-based
SizedBox(width: 5.w)      // 5% of screen width
SizedBox(width: 50.w)     // 50% of screen width

// Height-based
SizedBox(height: 3.h)     // 3% of screen height
SizedBox(height: 10.h)    // 10% of screen height

// Font sizes
fontSize: 4.w             // Responsive font size
fontSize: 5.h             // Based on height

// Padding/Margin
padding: EdgeInsets.all(4.w)  // 4% padding all sides
```

### Real-time Streams
```dart
// User data stream
stream: databaseService.getUserDataStream(userId)
builder: (context, snapshot) {
  if (snapshot.hasData) {
    return UserModel(snapshot.data);  // Real-time update
  }
}

// Leaderboard stream
stream: databaseService.getLeaderboardStream()
builder: (context, snapshot) {
  if (snapshot.hasData) {
    return LeaderboardList(snapshot.data);  // Auto-refresh
  }
}
```

### QR Code Generation
```dart
QrImage(
  data: user.uid,        // What to encode
  version: QrVersions.auto,
  size: 200.0,
)
```

## 🔐 Authentication Flow

```
User Input
    │
    ↓
┌─────────────────────┐
│ Input Validation    │
│ - Email format      │
│ - Password length   │
│ - Fields not empty  │
└─────────────────────┘
    │
    ├─ Valid ─→ Firebase Auth
    │               │
    │               ├─ Create Auth User
    │               ├─ Create Firestore Doc
    │               └─ Return Success
    │
    └─ Invalid ─→ Show Error Message
```

## 📱 Bottom Navigation Implementation

```dart
BottomNavigationBar(
  currentIndex: 0,    // Active tab index
  onTap: (index) {
    setState(() {
      selectedIndex = index;  // Update active tab
    });
  },
  items: [
    BottomNavigationBarItem(
      icon: Icon(Icons.home),
      label: 'Home',
    ),
    BottomNavigationBarItem(
      icon: Icon(Icons.dashboard),
      label: 'Dashboard',
    ),
    BottomNavigationBarItem(
      icon: Icon(Icons.person),
      label: 'Profile',
    ),
  ],
)
```

## 🎨 Color Application

```
┌─────────────────────────────────┐
│ Gradient Background             │
│ Top: Primary Light (#3F51B5)    │
│ Bottom: Primary Dark (#1A237E)  │
└─────────────────────────────────┘
         │
         ├─ Accent Yellow (#FFD740)
         │  - Buttons
         │  - Icons
         │  - Highlights
         │
         ├─ White (Opacity 0.1)
         │  - Containers
         │  - Cards
         │
         └─ Grey (Various opacity)
            - Text
            - Borders
```

---

**This architecture ensures:**
✅ Clean code organization  
✅ Scalability for future features  
✅ Real-time data synchronization  
✅ Responsive design on all devices  
✅ Secure Firebase integration  
✅ Smooth user experience  
